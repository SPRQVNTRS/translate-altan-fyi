# The @sprqvntrs/* packages this app depends on are published to npmjs, not to
# GitHub Packages. Pin the scope here so an install needs no token, even on a
# machine whose ~/.npmrc still routes the scope to npm.pkg.github.com.
@sprqvntrs:registry=https://registry.npmjs.org/
