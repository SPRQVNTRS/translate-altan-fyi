node_modules
.pnpm-store
*.pgsql
*.log
.env
.DS_Store
/node_modules/

# React Router
/.react-router/
/build/

# Claude Code agent worktrees (transient)
.claude/worktrees/

# tracker working/state files (regenerated by drift-check hook)
.tracker/.pending-sync

# Local copies of the upstream open-data dumps the importers read.
# Gigabytes of Wikidata / Tatoeba / PanLex, fetched by hand with curl.
# The importers never download; they take a `--file` path into here.
.data/

# Dictionary seed dumps produced by scripts/dictionary-dump.sh.
*.dump
*.dump.md5
