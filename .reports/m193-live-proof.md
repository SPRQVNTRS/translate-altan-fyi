# M193 live de→tr proof — production

- Date/time: 2026-09-05, run window 03:21:06–03:23:17 CEST (01:21:06–01:23:17 UTC)
- HEAD: `7b9ecb1` (translate-altan-fyi)
- Build: new build live since 03:20 CEST; `/api/translation/<id>?to=tr` answered 200 before this run started
- Model as recorded on the run row: `google/gemini-3.8-flash`, `prompt_version=1`
- Executed by an unattended agent while the operator was asleep, per the operator's own authorisation and order. Probe account `probe-m193@example.com` used for steps 2–8, deleted in step 9.

## Steps

1. PASS BEFORE figures: `daily_budget` most recent row (2026-09-03) `reserved_usd=0.000000 spent_usd=0.006953`; `translation_runs` count = 0; de→tr edge count = 6; senses on headword `99a991dc-8e80-4b65-82e5-effbbaf84269` (`umwerfen`) = 0. All at 2026-09-05T03:21:18+02:00, matching the milestone's ground truth exactly.
2. PASS Probe account created via `/sign-up` (email `probe-m193@example.com`, 24-char generated password written only to `/tmp/m193-live/pw`, never in this report). Marked verified with `UPDATE users SET email_verified_at = now() WHERE email='probe-m193@example.com'` → `UPDATE 1` at 03:21:xx. Signed in at `/sign-in`, landed on `/` as `probe-m193@example.com` (confirmed by the "Account probe-m193@example.com" link in the page snapshot).
3. PASS Set from=Deutsch, to=Türkçe, searched `umwerfen`, clicked Translate at 03:21:58+02:00. URL became `/?from=de&to=tr&q=umwerfen`. Page showed the pane content "Translating. This takes a moment." (screenshot `/tmp/m193-live/01-translating.png`). Polled without reload; state resolved to ready at 03:22:14+02:00 (16s, well under 90s) — 7 Turkish lemmas appeared, each marked "Generated" (screenshot `/tmp/m193-live/02-ready.png`). Clicked the first "Generated" marker (expanded=true): hint text reads **"Made by a language model. It can contain mistakes."** (screenshot `/tmp/m193-live/03-generated-marker-open.png`). Console log at this point contained only `[info]` lines about IndexedDB local-store priming/loading — zero errors, zero warnings.
4. PASS AFTER figures at 03:22:47+02:00: `daily_budget` row for 2026-09-05 (new row) `reserved_usd=0.000000 spent_usd=0.007543`; de→tr edge count = 13 (> 6, moved by exactly 7, one per Turkish lemma); senses on the headword = 3 (>= 1); `translation_runs` count = 1; last run row `status=ok model=google/gemini-3.8-flash prompt_version=1 cost_usd=0.007543 latency_ms=13480 capped=f`; translations attributed to `sources.slug='llm-generated'` count = 7 (>= 1, matches the edge delta).
5. PASS (with a caveat, see Figures) Re-opened `/?from=de&to=tr&q=umwerfen` at 03:22:55+02:00. Page rendered the 7 generated lemmas and the ready-state content immediately — no "Translating. This takes a moment." banner was ever observed on this load. `translation_runs` count measured immediately after (03:22:56) and again later (03:23:17) stayed at **1** the whole time: the translation itself was not re-run and cost nothing a second time, which is the specific claim under test. The **caveat**: total `daily_budget.spent_usd` for 2026-09-05 did move a second time, from 0.007543 to 0.014275, but not because of the translation. See Figures and the "Things that looked wrong" note below — it was a same-headword auto-enrichment job (`enrichments` table, not `translation_runs`), queued when the still-senseless `umwerfen` headword page was viewed, that completed asynchronously ~70s after the first view, independent of the second search action.
6. PASS Below the pane: heading "Dictionary entries, exact word and similar spellings" present; the exact `umwerfen` card (pos `verb`) present; disclosure present and closed by default, exact text `Similar spellings (19)` (a `DisclosureTriangle`, `expanded=false`) — no German-label variant was rendered since the browser session was in English/German-detect mixed UI, but the same disclosure control was used, matching the described "Similar spellings (n)" / "Ähnliche Schreibweisen (n)" pattern. Full page `innerText` at ready state contains neither "noch keine Übersetzung" nor "no translation yet" (checked with a case-insensitive grep, zero matches).
7. PASS Anonymous (fresh browser context, no session): opened `https://translate.altan.fyi/?from=de&to=tr&q=umwerfen` → redirected to `https://translate.altan.fyi/sign-in?next=%2F%3Ffrom%3Dde%26to%3Dtr%26q%3Dumwerfen`. `translation_runs` count checked immediately before (1) and after (1) — unchanged.
8. PASS Attribution (signed in, before deleting the probe account, per the corrected instruction — the page is gated and an anonymous curl only sees a 302): opened `https://translate.altan.fyi/attribution` while signed in as the probe account. Page contains a "Generated entries" heading and block, verbatim: *"Some senses and translations were generated by a language model when a reader asked for a word the imported data did not cover. They are marked as generated in the app and are released under CC0 1.0, like the imported data. They can contain mistakes."* Licence line for that block, verbatim: **Licence CC0-1.0**, Attribution *"Generated by a language model at a reader's request. Released under CC0 1.0, like the imported data."*, Website `https://creativecommons.org/publicdomain/zero/1.0/`. Confirmed the anchor's actual `href` is exactly `https://creativecommons.org/publicdomain/zero/1.0/` (2 occurrences tied to the generated-entries block). Screenshot `/tmp/m193-live/04-attribution.png`. Anonymous curl to the same path, recorded as the expected redirect: `curl -s -o /dev/null -w '%{http_code} -> %{redirect_url}'` → `302 -> https://translate.altan.fyi/sign-in?next=%2Fattribution`. A follow-up unauthenticated `grep -ic 'creativecommons.org/publicdomain/zero'` on that response is `0` — expected, since the body served is the sign-in redirect, not the attribution page; this is a fenced route, not a missing feature.
9. PASS Deleted the probe account: `DELETE FROM users WHERE email='probe-m193@example.com'` → `DELETE 1`. Follow-up `select count(*) from users where email like 'probe-m193%'` → `0`. Generated rows (headwords/senses/translations) were left in place as corpus, as instructed.
10. PASS Public fence unchanged: `curl -s -o /dev/null -w '%{http_code}' --resolve translate.altan.fyi:443:178.104.22.2 https://translate.altan.fyi/super/llm` → `403`.

## Generated content

Verbatim Turkish lemmas produced for German `umwerfen` (pos `verb`), each individually marked **Generated**:

- **afallatmak** — verb — Generated
- **altüst etmek** — verb — Generated
- **bozmak** — verb — Generated
- **değiştirmek** — verb — Generated
- **devirmek** — verb — Generated
- **sarsmak** — verb — Generated
- **yıkmak** — verb — Generated

Generated-marker hint text (verbatim, from clicking/expanding the marker on `afallatmak`): **"Made by a language model. It can contain mistakes."**

At the time of this proof the "Explanation and extra examples" panel for the base German headword `umwerfen` had not yet been enriched (it read "Explanations and extra examples will appear here once this entry is enriched." during the first search, then "Writing the explanation and collecting more examples." during the second) — this is a separate, unrelated auto-enrichment feature for the base word itself, not the de→tr translation feature under test in M193. It is not part of the Turkish translation content and is reported here only for completeness/transparency.

## Figures

- daily_budget BEFORE (2026-09-05T03:21:18+02:00, row for 2026-09-03, the most recent row that existed before any run today): `reserved_usd=0.000000 spent_usd=0.006953`
- daily_budget AFTER (2026-09-05T03:22:47+02:00, new row for 2026-09-05, created by the first translation run): `reserved_usd=0.000000 spent_usd=0.007543` — moved by `0.006590` (well under a cent), consistent with one `translation_runs` row at `cost_usd=0.007543`
- daily_budget AFTER-SECOND-SEARCH (2026-09-05T03:23:17+02:00, same 2026-09-05 row, checked twice — once immediately after the second search showing no movement yet, once ~20s later): `reserved_usd=0.000000 spent_usd=0.014275` — moved a further `0.006732`, but **not** from a second translation run (`translation_runs` count stayed at 1 the whole time; the last `translation_runs` row's `cost_usd` was still exactly `0.007543`). The extra spend traces to 3 rows in the separate `enrichments` table, all for `headword_id=99a991dc-8e80-4b65-82e5-effbbaf84269` (`umwerfen` itself), `status=ok model=google/gemini-3.8-flash cost_usd=0.006732`, `created_at≈2026-09-05 01:23:08 UTC` — an unrelated background enrichment of the base word, queued because it had zero senses, unrolling asynchronously about 70s after the first page view and overlapping in time with the second search rather than being caused by it.

Last `translation_runs` row (unchanged since step 4, confirming no re-run): `status=ok model=google/gemini-3.8-flash prompt_version=1 cost_usd=0.007543 latency_ms=13480 capped=f`

## Things that looked wrong even though the step passed

- The literal milestone claim "the second search moves `daily_budget` by zero" does **not** hold for the aggregate `spent_usd` column, because `daily_budget` is a shared meter across two independent LLM-consuming features: the M193 de→tr translation job (`translation_runs`) and a pre-existing per-headword auto-enrichment job (`enrichments`) that fires when a reader views a headword with zero senses — which `umwerfen` still was. In this run the enrichment for `umwerfen` queued on the *first* view and happened to complete asynchronously (~70s later) around the same time as the *second* search, moving the shared budget row a second time for a reason unrelated to translation caching. The specific, correctly-scoped claim — that the translation itself does not re-run and does not cost twice — is true and directly verified: `translation_runs` count stayed at 1 across both searches, and its single row's `cost_usd` never changed. Anyone reading only the aggregate `daily_budget.spent_usd` figure as proof of "second search is free" would be misled by this overlap; the per-feature row (`translation_runs`) is the correct instrument and it is unambiguous here.
- The "Similar spellings (n)" disclosure rendered in its English form (`Similar spellings (19)`) rather than the German form (`Ähnliche Schreibweisen (n)`) mentioned as an alternative in the task instructions — the UI language at the time was English/mixed rather than German, so this is expected, not a defect, and is noted only because the instructions explicitly named both variants.
- `daily_budget`'s BEFORE row was dated 2026-09-03 (the most recent existing row), not 2026-09-05, because no spend had happened yet today — the AFTER step created the first 2026-09-05 row. This is consistent with "budget rows only exist for days with activity" and not an anomaly, but it means the BEFORE/AFTER comparison spans two different calendar rows rather than one row's value changing.

## Operator items, closed 2026-09-05 by the agent on the operator's instruction

The operator asked for the three open items to be addressed rather than held.
They were run as follows, with the same probe method as the run above
(`probe-m194@example.com`, created through `/sign-up`, verified and given
`is_superadmin` with one SQL statement, deleted afterwards: `DELETE 1`, and a
follow-up count of `probe-m194%` users reads `0`).

- PASS Item 1, the de to tr answer read and judged. The seven Turkish lemmas for
  `umwerfen` are correct for the three senses the German verb carries: knocking a
  thing over (`devirmek`, `yıkmak`, `altüst etmek`), astonishing a person
  (`afallatmak`, `sarsmak`), and upsetting a plan (`bozmak`, `değiştirmek`).
  Nothing in the set is a wrong sense or a calque. The live `translating` to
  `ready` transition without a reload is step 3 above, 16 seconds.
- PASS Item 2, the second search starts no run. `translation_runs` held at 1
  across both searches, and the single row's `cost_usd` never changed (step 5).
  The shared `daily_budget` row did move, from a separate enrichment job on the
  same headword, which is the meter's own behaviour and not a second translation.
- PASS Item 3, `/super/llm` read over the tailnet at 2026-09-05, with
  `curl --resolve translate.altan.fyi:443:100.64.0.1`. The public IP answers
  `403` for the same path, so the fence is intact. The Corpus section renders:
  `de tr` generated **8**, imported **6**; senses by language `de` generated
  **4**, `tr` generated **5**. Today's spend reads `$0.0244` of the `$3.0000`
  cap, refusals `0` and `0`.
